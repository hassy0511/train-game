# Phase 7: 2章の仕上げ — 計画

最終更新: 2026-09-25
状態: GO（2026-09-25、だいさん「2章＋仕上げまで」「おすすめで進める」）。§6 の決めてほしいことは、すべておすすめで進める。
進める順: 軽量化（§2）を 2-2・2-3 より先に → 2-2 → 2-3（§1 のボタンを含む）→ 章のおわり（§3）→ きろく（§4 の 1・2・4）→ 入口のみがき（§4 の 5・6・8・9・10）→ とちゅうから つづける（§4 の 3・7）

## 0. まとめ
- **(1) ボタン**: カメラは右上の小さい丸に移し、一時停止のとなりに置く。右手側のボタンは「ライト｜ロケット／ジャンプ｜きてき」の 2×2 になる。レバーを右にしたときは左右を反対にする。1 章のステージもふくめて全部のステージで同じ配置にする。この変更は 2-3 の PR に入れる。
- **(2) 軽さ**: 実際に測ると、予算を超えているのは 1-1 のよこから（**250 回**・7.0 万）、1-2 のうんてんせき（168 回・**11.3 万**）、1-2 のうしろから（**208 回・12.6 万**）の 3 つ。原因は「モデルの色の数だけ描いている」ことと、「翼竜の群れ」。直すと、それぞれ約 55 回／約 75 回・8.1 万／約 80 回・8.9 万になる見込み。
- **(3) 2章のおわり**: 2-3 からはじまりの街へ最後の線路がのびて、6 つの島が輪になる。光が輪をひとまわりして、札「2しょう クリア！」が出る。そのあと、3 章の「？」の島が 1 つだけ出てくる。
- **(4) 仕上げの 1 番**: ロケットが要る「？」の記録が、今のしくみではロケットを覚えても取れない。これは約束やぶりになるので、いちばん先に直す。

---

## 1. カメラを右上へ、ロケットを 4 つめの席へ

### 配置
```
レバー ひだり（いつもの配置）
┌──────────────────────────────────────────────┐
│[にもつ]           [ はやさ ]          (カメラ)(||) │ ← 52px の丸 2 つ、すきま 12px
│              [ ていしゃ ゲージ ]  [カメラの えらぶ板]│ ← 右上の下にひらく
│┃レバー┃                         (ライト)(ロケット) │
│┃      ┃                         (ジャンプ)(きてき) │
└──────────────────────────────────────────────┘
レバー みぎ（左ききの配置）
│(||)(カメラ)          [ はやさ ]          [にもつ]│
│[カメラの えらぶ板]                              │
│(ロケット)(ライト)                     ┃レバー┃ │
│(きてき)(ジャンプ)                     ┃      ┃ │
```
- 一時停止は今の隅から動かさない。カメラはその内側に置く（右利き `right: safe-right + 80px`、左利き `left: safe-left + 80px`、高さは一時停止と同じ `top: safe-top + 12px`）。
- 置いた場所どうしはぶつからない。荷物の帯は反対の隅にあり、「はやさ」と停車ゲージは真ん中にある。停車ゲージは 1194 幅で x 277〜917、カメラの丸は x 1062〜1114。
- カメラの丸は絵だけにする（aria-label は「カメラ」）。押すと今と同じ 4 枚の選ぶ板が出る。板は隅の下 `top: safe-top + 76px` に、一時停止と同じ端にそろえて横 1 列でひらく。背の低い画面（`max-height: 500px`、iPhone SE）では板を 80×72 に小さくする。板を選ぶか、ほかの場所をタップすると閉じる（今と同じ）。
  - 開いた板は 停車ゲージと同じ高さに かかる（iPad では停止線の下半分、小さいスマホでは ほぼ全部）。板は画面の上の DOM の いちばん はじめに置き、停車ゲージ（と吹き出し・暗転）が 板の上に描かれるようにした（ゲージは さわれないので、下の板は そのまま押せる。実装で決めた）
- ロケットは、前にカメラがあった「上の外側」の席に入る。
  - きてきとジャンプは、子どもがもう覚えた場所から動かさない。
  - ロケットは坂のふもとなどで落ち着いて押すボタンなので、一瞬を争うジャンプに一番押しやすい「下の内側」を残す。
  - ロケットを覚える前は、その席が空いたままになる（1-1 はきてき 1 つだけ）。
- ロケットの丸は見た目と押し口だけをここで決める。しくみそのものは 2-3 の設計で決める。
  - ふちの輪がゲージ（今のクールダウンの輪を使う）。
  - 噴かしている間は `data-boost=1` で炎がゆれ、押しても何も起きない。
  - ゲージが空か「ロケット禁止」の区間では `data-idle=1` で灰色になる。押すとピコのせりふ（`lines.rocketNo`）だけが出る。
  - 「いま！」のときは `data-glow=1` で光る。
  - 押し方は pointerdown で 1 回押すだけ。長押しはしない。

### ファイルごとの変更
- `src/ui/camera-button.ts`: 置き先を `uiEl` にする。class を `corner-button` にし、ラベルの文字をやめて絵だけにする。選ぶ板は `root.parentElement` ではなく `root` に足す。
- `src/ui/pause.ts`: class を `corner-button pause-button` にし、形を共通にする。
- `src/ui/ability-buttons.ts`: `createRocketButton(root, onPress): { show(); set(gauge, boosting, glow, idle) }` を足す。作りはジャンプと同じ。
- `src/ui/styles.css`:
  - `.corner-button` を共通にする（今の `.pause-button` の中身）。`#camera.corner-button { right: calc(var(--safe-right) + 80px) }`。
  - `.camera-menu` は `top` で置き、`bottom: auto` にする。
  - `.action-buttons` の並びを `'light rocket' 'jump whistle'` に、左利きは `'rocket light' 'whistle jump'` にする。
  - `#rocket { grid-area: rocket }`、`.ring.is-rocket`、炎の CSS を足す。
  - 左利き用に `#camera` と `.camera-menu` の左右反対の指定を足す。`#camera { grid-area: camera }` は消す。
- `src/main.ts`: `createCameraButton(uiEl, …)` に変える。ロケットをつなぎ、`showAbility('rocket')` を足す。テストコース 0-0 の能力に `rocket` を足す。
- `src/mission/runner.ts`: `ABILITY_NAMES` に `rocket: 'ロケット'` を足す（今は無い）。
- `tests/smoke/pause-settings.spec.ts`: 左右両方の配置で次を確かめ、`corner-right.png`・`corner-left.png` を撮る。
  - カメラと一時停止の高さがそろい（±2px）、重ならない。
  - カメラが一時停止の内側にある。
  - 選ぶ板が画面の中に収まり、右手側のボタンと重ならない。
  - 1194×834 と 667×375 の両方で確かめる。
- `tests/smoke/smoke.spec.ts`（0-0）: 丸が 4 つ出て重ならないこと、見えている丸が 4 つ以下であることを確かめる。左右両方でスクショを撮る。
- `monkey.spec.ts`: 押すボタンに `#rocket` を足す。`#camera` は id のままなので `stage-1-1.spec.ts` は変えなくてよい。
- 文書: GAME_SPEC §4 の表を直す（右手の丸は「きてきは いつも。ライト・ジャンプ・ロケットは覚えたら」、右上に「一時停止・カメラ（小さい丸）」）。CLAUDE.md の「丸ボタン最大 4 つ」はそのまま守れている。

---

## 2. 重い場面の軽量化

### 測ったこと
- 日付と条件: 2026-09-25。開発ビルド、手元の Chromium（ソフトウェア描画）、1194×834、倍率 1。
- 方法: 電車を 50 m ごと（駅の近くは 20 m ごと）に置き、カメラを合わせて 1 コマ描く。そのときの描画回数と三角形の数を、モデルごとに数えた。
- 使ったスクリプト: 仕上げ PR1 で `scripts/probe-budget.mjs`（`npm run budget`）として入れる。
- この方法だと、スモークの通しより重い場面を拾う。前に出ていた「約 203 回／約 11.4 万」との違いはそのため。

| 場面 | 一番重いコマ | 予算 |
|---|---|---|
| 1-1 よこから | **250 回**・7.0 万（s=300） | 200 回・10 万 |
| 1-1 うしろから | 190 回 | |
| 1-2 うんてんせき（きょうりゅうえきの手前 s=550） | 168 回・**11.3 万** | |
| 1-2 うしろから（s=640） | **208 回・12.6 万** | |
| 1-3 うしろから／うえから | 186 回／183 回 | |
| 2-1（どのカメラでも） | 164 回・6.1 万まで | |

**1-1 よこからの内訳（250 回）**
- 町の家と木: 104 回（house-a は 8 色 × 3 区画で 24 回など）
- 駅 4 つ: 80 回（1 駅 20 回 = ホーム 6、屋根 5、駅名板 4、とまれ板 4、白線 1）
- 踏切: 4 回
- 電車とピコ: 44 回（先頭 15、客車 10×2、ピコ 9）
- 線路 8 回、猫と乗客 8 回、空と地面 2 回

原因: GLB のマテリアルは色だけで、絵（テクスチャ）がない。それでも「1 色 = 1 回」で描いている。

**1-2 うんてんせき s=550 の内訳（11.3 万）**
- 翼竜の群れ 2.86 万（13 羽 × 2,200 三角形）。群れは画面の外にいても毎回描く設定（`frustumCulled = false`）。
- 崖 2.9 万、ソテツ 0.95 万、シダ 0.71 万、恐竜 4 頭 1.73 万、線路 1.18 万。
- 描画回数 168 の内訳: 地形 95、駅と恐竜など 52。

### 直し方
**A. 色を頂点に焼いて、1 回で描く（全ステージに効く。いちばん大きい）**
- 新しいファイル `src/view/three/bake.ts` を作る。
  - 対象: 絵がなく、不透明なマテリアルだけでできたモデル。町・駅・谷・電車・ピコの GLB と、コードで描いた仮のモデルがこれに当たる。
  - 部品を全部 1 つの形にまとめる。各頂点に `color`（マテリアルの色）と `bakedRoughness` を持たせ、uv は捨て、index のあるなしをそろえる。
  - マテリアルは共通の `MeshStandardMaterial({ vertexColors: true, metalness: 0 })` を 1 つだけ使う。onBeforeCompile で roughness を頂点から読む（15 行ほど）。これで見た目は今と同じになる。
  - 絵のあるモデル（恐竜、猫、翼竜、サカサ）は今のまま。
  - 作った結果は、元のモデルごとに WeakMap で覚えておく（`merged` と同じやり方）。
- `src/view/three/props.ts` の `sourceMeshes()`: 焼けるモデルは焼いた 1 つを返す。これで「モデル × 区画」ごとに 1 回になる。
- `src/view/three/actors.ts` の `addStations()`: 1 駅の部品 5 つを 1 つに焼く。1 駅 1 回で描き、見えない駅は駅ごとに描かない（今のコメントの方針のまま）。踏切も焼く。
- `src/view/three/ThreeSceneView.ts` の `init()`: 電車・客車・ピコを焼く。どれも動かすのは丸ごとだけ。ドアの帯やライトの光は別の物なので影響しない。
- 減る量: 1-1 よこからで −76（町）、−79（駅と踏切）、−40（電車）。1-2 で −55（地形）、−30（駅）。

**B. 区画を細かく（150 m → 75 m）**
- `props.ts` の `CELL = 75` にする。A のあとは 1 区画 1 回で済むので、細かくしても安い。そのぶん画面の外のものを描かずに済む。
- 見積もり（probe2）:
  - 1-2 s=550: 地形 4.67 万 → 3.91 万（−0.76 万）。
  - 1-2 s=640: 5.32 万 → 4.05 万（−1.27 万）。
  - 描画回数は 30 → 40 回に少し増えるが、それでも少ない。

**C. 翼竜の群れ（1-2）**
- `src/view/three/abilities.ts` の `Flocks`: 群れの円ぜんぶを包む大きさを固定で決めて、`frustumCulled = true` にする。カメラの後ろにいる群れは描かなくなる。
- 遠く用の軽い翼竜 `ptero-far` を作る。
  - 400 三角形以下、1 色で絵なし、形は同じ。群れはいつも 40 m 以上離れているので、見分けはつかない。
  - `assets/blender/dinos.py` に足し、`assets/models.json` に書く。
  - `src/stages/1-2.json` の群れを `"model": "ptero-far"` にする。
- 減る量: 見えているときで 2.86 万 → 0.39 万（−2.47 万）。軽くなるので、群れを前の 10＋8 羽に戻せる（0.72 万）。

**D. （してもしなくてもよい）** ソテツを 868 → 約 450 三角形にする（`valley.py`、葉の段を減らす）。−0.3 万。

### 直したあとの見込み
| 場面 | いま | A〜C のあと |
|---|---|---|
| 1-1 よこから | 250 回・7.0 万 | **約 55 回**・7.0 万 |
| 1-2 うんてんせき s=550 | 168 回・11.3 万 | **約 75 回・8.1 万** |
| 1-2 うしろから s=640 | 208 回・12.6 万 | **約 80 回・8.9 万** |

1-1 うしろから（190 回）と 1-3（186／183 回）も、60〜100 回くらいまで下がる見込み。2-2・2-3 で物を増やす余裕ができる。

### これからの見張り
- `scripts/probe-budget.mjs` を作り、`npm run budget` で動かす。
  - 開発サーバーで、全ステージ × 4 カメラ × 線路 50 m ごとに測る。一番重いコマと内訳を出し、200 回・10 万を超えたら失敗にする。
  - 開発版だけにある `__debugView` と `__debugTrain` を使うので、本番には入らない。
  - 速さの都合で、最初は CI に入れない。ステージの PR の前に回す。
- 通しのスモークは、重さをログに出すだけでなく `expect(...).toBeLessThanOrEqual(200)` で確かめるようにする。
- PHASE4_DESIGN §6 の表と TECH_SPEC §6 に新しい数字を書く。
- 確認: `https://hassy0511.github.io/train-game/models.html?compare=ptero,ptero-far`、1-1 は本番 URL の「よこから」で見る。

---

## 3. 2章のおわり（地図）

### 流れ（2-3 のクリア札「つづく」のあと）
1. 地図が開く。最後の線路が 2-3 からはじまりの街へ、下側をまわってのびる。6 つの島が輪になる。
2. 金色の光が 1-1 から順に輪をひとまわりする。光が通るとき島がぴょこっと跳ね、きらきら音が鳴る（約 3 秒）。
3. 札を出す。
   - 絵: 新しい `ring`（線路でつながった 6 つの点の輪）
   - 文: 「2しょう クリア！／いきものの せかいが／ぜんぶ つながった！」（どの行も 20 字以内）
   - ボタン: 「やったね！」
   - ファンファーレを鳴らす。
4. 札のあと、左上に白い「？」の島が 1 つ浮かぶ。はじまりの街から点線がのび、名前は「3しょう」。タップすると揺れて、小さい吹き出し「つづきは また こんど！」が 2 秒出る。
5. 閉じるボタンは「タイトルへ」。タイトルに「1しょう ★ 2しょう ★」を出す。

1 章のおわりにも同じしくみで小さく出すのがおすすめ（決めてほしいこと 1）。1-3 から 2-1 への線路がのびたときに、札「1しょう クリア！／つぎは いきものの せかい！」を出す。

### データ（`src/world/world.json` と `src/world/types.ts`）
```json
"chapters": [
  { "id": 1, "title": "1しょう はじまり",
    "finale": { "link": "1-3>2-1", "card": "1しょう クリア！\nつぎは いきものの せかい！", "button": "つぎへ" } },
  { "id": 2, "title": "2しょう いきものの せかい",
    "finale": { "link": "2-3>1-1", "ring": ["1-1","1-2","1-3","2-1","2-2","2-3"],
                "card": "2しょう クリア！\nいきものの せかいが\nぜんぶ つながった！", "button": "やったね！", "icon": "ring" } },
  { "id": 3, "title": "3しょう みずとこおり",
    "teaser": { "label": "3しょう", "x": 16, "y": 26, "from": "1-1", "after": "2-3", "line": "つづきは また こんど！" } }
],
"links": [ …, ["2-3", "1-1", { "via": [35, 88] }], ["1-1", "teaser:3", { "via": [8, 46] }] ]
```
- `via` は曲がり方の目印の点（%）。今の決め方（中点の上）だと、縦長の線や輪を閉じる線がうまく曲がらないので足す。値は絵を見て合わせる。

### ファイルごとの変更
- `src/ui/map.ts`:
  - 線路の線は、`via` があればそれを曲がりの目印にする。
  - `MapOptions.finale?: { ring: string[]; onShown(): Promise<void> }` と `teaser?` を足す。
  - 線路がのび終わったら（1.4 秒）`.is-finale` を付ける。線路と島に順番の変数 `--i` を持たせ、光と跳ねを順に動かす。
  - 光がまわり終わったら `onShown()`（札）を待つ。
  - そのあと `.map-island.is-teaser`（絵は unknown.png）と点線をふわっと出す。タップしたら揺らして `.map-say` の吹き出しを出す。
- `src/main.ts` の `openMap()`:
  - 新しく敷いた線路に、どれかの章の `finale.link` が入っていたら finale を渡す。`onShown` では `audio.playFanfare()` を鳴らし、`showCard(uiEl, card, button, icon)` を出す。
  - その線路は、札を閉じたあとに `mapLinks` に保存する。途中でアプリを閉じたら、もう一度見られる。
  - `after` の島がクリア済みなら teaser を渡す。
- `src/ui/cards.ts`: 絵に `'ring'` を足す。`.card` に `z-index: 40` を付ける（地図は 30 なので、今のままだと札が地図の下に隠れる）。
- `src/ui/styles.css`: 光、跳ね、teaser、吹き出しの CSS を足す。
- `src/ui/title.ts` と `main.ts`: 章の★を出す。world.json の島がぜんぶ `cleared` に入っていれば★にする。
- 音: `playFanfare` ときらきら音（`playRecord`）を使い回す。新しい曲は作らない。
- `tests/smoke/map-finale.spec.ts`:
  - `addInitScript` でセーブを入れる（6 ステージクリア済み、`2-3>1-1` だけ未表示）。
  - タイトルの「ちず」を開き、線路がのびること、`#card` に「2しょう クリア」が出ること、`.is-teaser` が見えることを確かめる。タップで `.map-say` が出ることも確かめる。
  - 地図を開き直したら、おわりの演出が出ない（1 回だけ）ことを確かめる。
  - スクショ `map-finale-ring.png`、`map-finale-teaser.png` を撮る。
  - 2-3 の通しのテストも、最後はこの流れで終える。
- 2-2・2-3 の島の絵は、それぞれのステージの PR で `render-map` に足す。

### 実装メモ（仕上げ PR2、2026-09-26）
- 光は、輪の線路 1 本ごとに 0.5 秒で走る金色の短い線（`.glow`）。島は光が着いたときに跳ねて金色に光り、そのまま光ったままになる（輪ぜんぶが光って札に入る）。1-1 は はじめと さいごの 2 回 跳ねる。きらきら音は跳ねるたびに鳴る（7 回）
- 1 章のおわりは光の輪なしで、線路がのび終わったらすぐ札。音は ファンファーレではなく いつもの札の音（`playCard`）にした（「小さく」の解釈）。2 章は ファンファーレ
- おわりの演出の間（線路がのびる〜札を閉じる）は、閉じるボタンを隠し、島もタップできない。とちゅうで止まって見えないようにするため
- 「？」の島は白くして ゆっくり上下にただよう。吹き出しは島の右上に 2 秒
- タイトルの章の★: どれかの章が おわるまでは出さない（はじめての子に「☆☆」を見せない）。おわったら「1しょう ★ 2しょう ☆」のように、島のある章を全部ならべる
- おわりの演出は、その章の島（2 章は輪の 6 つの島）が ぜんぶ クリアずみに なってから 出す。`?stage=` で最後のステージだけ遊んだときは、線路は かくが、新しい線路としては あつかわず、保存もしない。章が本当に おわったときに はじめて のびて 札が出る。「？」の島も、`after` の島の章が おわってから（レビューのあと）
- おわりの札のボタンは 0.9 秒あとに ぽんと出る。演出の間 タップしつづけていた子が、札を見ないまま 閉じてしまわないように（レビューのあと）
- 札を地図の上に出す `z-index: 40` は、地図の上の札だけにした（`.map ~ .card`）。ステージの中では 一時停止の画面が 札より上のまま（レビューのあと）
- `via` は 2 次ベジェの制御点（曲線が向かう点）として使う。値は計画のまま（2-3>1-1 は [35, 88]、1-1>teaser:3 は [8, 46]）で絵を見て問題なかった

### 3 章から先の地図
- **3 章ができるまで**: 「？」の島は 1 つだけにする。3-1〜3-3 の島は world.json に書かない。「？」が 9 つ並ぶと子どもには雑音になり、押しても揺れるだけの島が増えるから。
- **3 章を作るとき**: 地図をページに分ける。
  - 1 ページに 2 章ずつ: 1 ページめ = 1・2 章（今の地図のまま）、2 ページめ = 3・4 章、3 ページめ = さいご（まん中にさかさまの城）。
  - world.json に `chapters[].page` を足し、島の位置はページの中で決める。
  - 左右に大きい ◀ ▶ を置く。何かが分かっているページの方向だけに出す。
  - 地図は、次に行く島のページで開く。見出しにはそのページの章の名前を出す。
  - 「？」の島は、次のページへ出ていく線路に変わる。
  - 5-1 のエンディング（GAME_SPEC §7「全世界がつながる地図演出」）では、全部のページを縮めて 1 枚に並べて見せる。

---

## 4. そのほかの仕上げ（大事な順）

| 順 | 項目 | なぜ | 中身（ファイル） | 手間 |
|---|---|---|---|---|
| 1 | **ロケットの「？」記録を本当に取れるようにする**（1-2 がけの うえ、2-1 こずえの てっぺん） | `runner.updateRecords` はライト以外の `requires` を必ず飛ばすので、ロケットの記録は見つからない。2-3 でロケットを覚えると、地図の「？」は消えるのに取れないまま残る | 記録のルールを「その能力を使っている間に近くを通ると見つかる」にする（ライト = 照らしている間、ジャンプ = 空中、ロケット = 噴かしている間。`runner.ts`、STAGE_SCHEMA）。1-2 は 800 m 付近の分岐から崖を登る短い急坂と車止めを足し、上に記録を置く。2-1 はこずえのえきの先に、上向きの短い枝を足す。分岐の標識にロケットの絵を描き、ロケットがないときはピコ「ロケットが あれば のぼれそう…」。急坂とロケットは 2-3 のしくみをそのまま使う。記録の形（がけの上の空の巣、こずえの羽根）はコードで描く | 中（2-3 が前提） |
| 2 | **ずかんの絵** | 見つけた記録のうち 4 つ（くもの けっしょう、かざみどり、リスの すあな、おおきな どんぐり）が、今は絵なしで出る。これから増える記録もコードで描くモデル | `scripts/render-zukan.mjs`（render-map と同じ作り）で、記録ごとに 256px の絵を `public/zukan/<id>.png`（1 枚 20〜40KB）に書き出す。`zukan.ts` はそれを使い、sw の事前キャッシュにも入れる。並びは「島ごとの段」にして、見出しに「みつけた 11/18」。後の能力が要る「？」には、その能力の灰色の絵を付ける。ついでに `assets/previews` の glob をやめるので、配布物が約 10MB 減る | 小 |
| 3 | **とちゅうから つづける** | 1 ステージは約 10 分。途中でやめると、次はオープニングからやり直しになる | セーブに `resume: { stage, mission }` を足す（省略できる欄なのでスキーマ 1 のまま）。ミッション i は、前のミッションの最後の駅から始める。寸劇の「早送り」を作る: 線路を切る・能力・役者の出し入れだけを一瞬で済ませ、せりふ・カメラ・待ちは飛ばす（`cutscene/`、`runner.ts`）。タイトルは「つづきから（2-1 ミッション 2）」 | 中〜大 |
| 4 | **1-1 の記録 3 つ** | 仕様（GAME_SPEC §7）の抜け。1-1 だけ 0 個なので、ずかんが 15/18 までしか埋まらない | まちの かんばん（通れば見つかる）、ほんぶの けいじばんの せっけいず（通れば）、やねの うえの ふうせん（ジャンプ = 1 の「空中」ルール。1-1 では「？」、1-2 のあとに戻って取る）。形はコードで描く。`1-1.json` に書く | 小 |
| 5 | **よみこみ中の画面** | はじめての読み込みでは数秒まっくらで、止まったように見える | `index.html` に CSS だけの画面を足す（線路を走る電車の絵と「よみこみちゅう…」）。`data-ready` で消す | ごく小 |
| 6 | **タイトルを「ゲームの顔」に** | 今は青いグラデーションに文字とボタンだけ。ストアのスクショにもなる | 後ろで 1-1 の 3D をそのまま見せる（本部のホームに止まったワンダー号を、よこからゆっくり回る）。題字は 2 行にして線路の飾りとピコの絵を付ける。つづきがあれば「つづきから」を大きく、全部クリアなら「ちず」を大きくする（今は全部クリアすると「はじめる」しか残らない）。章の★（3 章）も出す（`title.ts`、`styles.css`） | 小 |
| 7 | **2 回目からは おはなしを とばせる** | 記録を取りに戻るたびにオープニングを見るのはつらい | クリア済みのステージだけ、寸劇の間に右上へ「▶▶」の小さい丸を出す。3 の早送りを使う | 小（3 のあと） |
| 8 | **セーブを守る** | Safari のタブで遊んでいると、しばらく開かない間にデータが消えることがある（ITP の上限。ホーム画面に追加したアプリは対象外）。兄弟で iPad を共有することもある | `navigator.storage.persist()` を頼む。9 のページに「ホーム画面に追加すると きえにくい」と書く。「あいことば」を作る: 進み具合を 12 文字ほどの記号にして書き写し、あとで入れれば戻る（通信なし）（`core/progress.ts`） | 小〜中 |
| 9 | **おうちの かたへ** | ストアに出すときに要る（通信しない・何も集めない、と書く）。データの消し方も要る | 設定に「おうちの かたへ」を足す。2 秒の長押しで開くので、子どもがうっかり開かない。中身: 方針、ホーム画面への追加のしかた、ビルド番号、「きろくを ぜんぶ けす」（2 回確かめる）（`settings.ts`） | 小 |
| 10 | **クリア札の ごほうび** | 何ができたかが見えると、もう 1 回やりたくなる | 「ぴたっ！ 5かい」「きろく 2/3」をアイコンで出す（`runner.ts` の札、`cards.ts`） | 小 |

---

### 実装メモ（仕上げ PR3「きろく」、2026-09-26）
- 記録の 決まり: 「その能力を 使って いる 間に 25 m 以内を 通る」。ロケットは「ふかして いる 間」に 加えて「その いきおいが 残って いる 間（レバーの 速さに 戻るまで）」と「いまの 線路か ひとつ 前の 線路で ふかした あと」も 数える。びゅーんで 分かれ道の 手前で ふかすと、いきおいは てっぺんの 前に 消えるのに 坂は のぼれて しまい、巣が 見つからなかった（レビューで 指摘、直した）。のぼれたなら かならず 取れる（STAGE_SCHEMA §11）
- 支線は 新しい 欄 `rails[].spur`: 車止めで 止まると 暗転して 本線の 分かれ道の 少し 先へ 戻る（失敗では ない。画面は しずまず ゆれない。ピコ「いきどまり！ もとの みちに もどるよ」）。`deadEnd`（はずれの 道）とは 分けた
- 分かれ道の 新しい 欄 `junctions[].needs`: わき道に 要る 能力。標識の 上に その能力の 丸い 札、矢印の ボタンの 角に 同じ 絵。能力が ないと 矢印は 灰色で えらべず、押すと ピコ「ロケットが あれば のぼれそう…」。はじめは 60 m 手前でも 言って いたが、1-2 では きてきの 合図と こどもの 恐竜の「ゆっくり！」、2-1 では こずえのえきの「ゆっくり！」を おくらせるので、押した ときだけに した（予告は 標識と 矢印の 灰色の ロケットの 絵）
- ロケットの「車止めの 150 m 手前は おやすみ」は、同じ 線路の 先に のぼり坂が 残って いる 間は かけない（支線を 短く する ため。てっぺんから 先は おやすみ）
- 1-2 の 支線は 800 m 付近の がけ（右の かべ）の 外側を のぼる。かべが 762 m から 始まるので、分かれ道は かべの 手前の 695 m に した（ねぼすけ恐竜 680 m の すぐ 先）。巣は 右の かべの 上（main 831 の 右 22 m）に あり、本線の 谷底からも 見える。すべったら main 686 へ（恐竜の 先。起きた 恐竜は よけた まま。止まった まま 矢印を 押せる）、とったら main 725 へ（ピコ「やったね！ もとの みちに もどるよ」）
- 2-1 の 支線は こずえのえき（top 240）が ステージの さいごの 駅なので、駅の 先には つなげられない。えきの 手前 top 165 から 右へ わかれ、えきの 横を 上へ のびて、えきより 先（奥）で 終わる えだに した
- 1-1 の 記録: かんばんは M2（さくら→みなと）の とちゅう、せっけいずは ほんぶの 前（M3 で ほんぶに もどる ときに 見つかる。M1 の はじめに いきなり 見つからない ように）、ふうせんは 右の 家の やね（ジャンプを 覚えて から 戻ると、60 m 手前で ピコ「ふうせんだ！ ジャンプで とどくかな」）
- 記録の 新しい 欄 `hint`: 取れる ときだけ 60 m 手前で 1 回（1-1 の ふうせん だけ。ロケットの 2 つは 坂の せりふと かさなり、見つけた あとに 出るので 付けない）
- 図鑑は 6 段で iPad の 画面より 長いので、「とじる」は 下に くっついて いつも 見える
- 図鑑: `scripts/render-zukan.mjs` → `public/zukan/<id>.png`（17 枚、7〜30 KB）。`assets/previews` の glob を やめて、配布物（dist）は 16 MB → 8.3 MB（プレビュー 73 枚・7.4 MB が 出なく なった）
- 記録の 数は 18（6 ステージ × 3）。地図の「？」は 今までどおり「まだ 持って いない 能力が 要る 記録が 残って いる」で 出る（1-1 は ジャンプを 覚えるまで「？」）

### 実装メモ（仕上げ PR4「入口のみがき」、2026-09-26）
- **よみこみ中の画面（5）**: `index.html` の中に CSS だけで書いた（空の色・3 両の電車・流れる枕木・「よみこみちゅう・・・」）。スクリプトより先に出る。`main.ts` が `data-ready` を立てたときに消す（CSS でも `#app[data-ready]` / `#app[data-error]` のときは隠れる）。電車に顔はなく、実在の車両に似せていない
- **タイトル（6）**: 後ろは そのステージの 3D をそのまま使う（新しい場面は作らない）。カメラは スタートの駅に止まった電車の まわりを、ホームと反対側から ±28° ゆっくり ゆれる（48 秒で 1 往復、半径 30 m・高さ 9 m）。ぐるっと 1 周にしなかったのは、1-1 では ホーム側に ほんぶの 建物が 近くて、カメラが 建物に 入って しまうから。どちら側かは スタートの駅の `platformSide` から決める（値は `TITLE_ORBIT`、`train/params.ts`）
  - 題字は 2 行（「ワンダーごうと」「ふしぎな せかい」）、下に 線路の 飾り（SVG）、左に ピコの 絵。ピコの 絵は ゲームの モデルから `node scripts/render-zukan.mjs --model partner public/title/pico.png 20 6` で 描いた（30 KB、sw の 事前キャッシュに 入れた）
  - 大きい ボタンは 1 つ: つづきが あれば「つづきから（…）」、全部 クリアなら「ちず」、ほかは「はじめる」。ほかは 下に 小さく 並べる。大きい ボタンの まわりに 白い 輪が ふわっと 広がる（ボタン 自体は 動かさない。動くと テストの クリックが 「止まっていない」で 失敗する）
  - タイトルの 間は 運転の ボタン（レバー・きてき・カメラ など）を 隠す（`#app[data-title="1"]`）。前は 不透明な タイトルが 上から かくして いた
  - 軽さ: タイトルは 前から 後ろで 3D を 描いて いた（運転席の 景色を 不透明な 画面で かくしていた）ので、描く 量は ほぼ 変わらない。`npm run budget` に「title」の カメラを 足した（電車の 両側 ±28°）。1-1 は 最大 41 回・5.6 万三角形（使わない ホーム側も ふくむ。ゆれる 側は 12 回前後）。全ステージで いちばん重いのは `?stage=2-3` で タイトルを 出したときの 84 回・8.2 万で、予算（200 回・10 万）の 中。通しの スモークでも タイトルの 間の 一番重い コマを 確かめる
- **セーブを守る（8）**: 保存するたび（1 回の 起動で 1 回）`navigator.storage.persist()` を頼む。「あいことば」は 12 文字（`XXXX-XXXX-XXXX`、数字と 英大文字。I・L・O・U は 使わず、入れるときは O→0、I・L→1 と 読みかえる。小文字・空白・ハイフンは 気にしない）。1 文字めが 版、中身は クリア・能力・記録・地図の 線路の 全部（1 つ 1 bit）、21 bit の 検査で 打ちまちがいを はじく（1 文字の まちがいは 全部 はじいた）。入れると 今の 記録と 入れかえる（確かめ 1 回）。仕組みは TECH_SPEC §3「進行セーブ」
  - かなの あいことば も 考えたが、手で 書き写すときに 似た 字（ぬ／め、わ／ね／れ、る／ろ）が 多いので 英数字に した
- **おうちの かたへ（9）**: 設定の 左下の 小さい「おうちの かたへ」を 2 秒 押しつづけると 開く（輪が たまる）。短く 押すと「2秒 長押し」が 出て ゆれるだけ（ふだんは 出さない。大人向けに 漢字で 書き、ひらがなを 読める 子に 開け方を 教えない）。中身: このゲームについて（どこにも 送らない・何も 集めない・広告や 購入なし）、記録を 消えにくく する 方法（ホーム画面に 追加。いまの 保存の 状態も 出す）、あいことば、「きろくを ぜんぶ けす」（2 回 確かめる。どの 確認も「はい」の ボタンは 0.8 秒 あとに 出るので、2 度 押しで 両方 すすまない。設定は 残る）、ビルド番号。大人が 読むので、ここだけ ふつうの 漢字まじりに した。記録を 入れかえた／消したあとは タイトルから やりなおす
- **クリア札の ごほうび（10）**: 札の 下に 2 つ。「ぴたっ！ 5かい」（この 回に ぴったり止まれた 数。0 のときは「とまれた！ 5かい」にして、0 を 見せない）と、その 島の 記録 3 つの 絵（見つけた ものは ずかんの 絵、まだは「？」、この 回に 見つけた ものは ぽんと 出る）＋「きろく 2/3」（1 つも まだの ときは「きろく さがしてみよう」にして、ここでも 0 を 見せない）
- テスト: `smoke.spec.ts` に よみこみ中の画面（物理エンジンの 読み込みを 止めて 見る）→ 消える → タイトルの 3D（`data-camera="orbit"`、運転の ボタンが 隠れて いる、重さ）→「はじめる」で もとに 戻る。`pause-settings.spec.ts` に 長押し（短く 押しても 開かない）、2 回 確かめて 消す（2 回めで「やめる」なら 残る）、あいことばの 往復（全部 入った 記録 → 消す → 小文字・空白・O で 入れる → もとどおり）、打ちまちがいは はじく。`stage-2-3-full.spec.ts` の クリア札で ごほうびを 確かめる。あわせて その `tapUntil` を 直した: 待って いる せりふが 出た 瞬間に 吹き出しを タップして 消して しまう ことが あった（おわりの「はしが おちちゃった…！」で 3 回中 2 回 落ちた）。いまは ページの 中で「ほかの せりふの ときだけ タップ」を 1 度に 行う

### 実装メモ（仕上げ PR5「つづき」、2026-09-26）
- **セーブ（3）**: `resume?: { stage, mission }` を足した（省略できる欄なので スキーマ 1 のまま。前の セーブも 読める）。`mission` は 0 から数えた「つぎに はじめる ミッション」。ミッションが おわった とき（「できた！」を 閉じて、その あとの 寸劇も おわった とき）に 書き、そのステージを クリアしたら 消す。寸劇の あとに したのは、1-2 の ライトを 覚える 寸劇の とちゅうで やめても、つぎは ミッション 2 から（ライトの 札と ピコの せつめいを 見のがさない）。さいごの ミッションでは 書かない（おわりの 寸劇の とちゅうで やめると、さいごの ミッションから やりなおし。おわりの 寸劇だけから はじめる 形は 作らなかった）
  - **わくは 1 つ、いま すすめて いる ステージの もの**（レビューで 直した）: クリア済みの ステージを 遊びなおしても 書かない（記録さがしで 1-1 に もどっても、2-3 の つづきは 消えない。遊びなおしには ▶▶ が ある）。同じ ステージを はじめから（「はじめから（2-1）」や 地図の 島から）遊んでも、前より 手前の ミッションには もどさない
  - クリア札の かず: つづきには それまでの 停車（`stops`・`perfect`）と、その 遊びで 見つけた 記録（`found`）も 入れる（どれも 省略できる 欄）。つづきから クリア すると、ぴたっ！の かずと きらきらの 新しい 記録は 最初からの 分に なる
- **はじまりの 形**: ミッション i は、ミッション i−1 の さいごの 駅に 止まって はじまる。オープニングと、それまでの ミッションの あとの 寸劇（1-2 の ライト、2-3 の ぐるぐる ぼうし）は 早送りで 済ませる。乗客と 荷物は 各 駅の 乗り降りを 足して 出す（2-2 ミッション 2 は 2 人 のせて、2-1 ミッション 3 は どんぐり つき）。カメラは いつもの 運転席
  - 花の橋（2-2）: 「ステージの 間 ずっと ひらいたまま」（STAGE_SCHEMA）なので、スタートから その駅までの 道に ある 花は ひらいた 形で 置く（2-2 ミッション 3 では 1・2 番めが ひらき、3 番め（くもの いとの 線路の 先）は とじた まま）。道は 分かれ道と 合流を たどって 探す。道は もう 戻らないので 遊びは かわらないが、うしろを 見ると 花の 橋が かかって いる。「とじた まま」に すると、うしろに とじた つぼみと まって いる ちょうちょが 見えて、ちぐはぐに なる
  - ねこ・恐竜・バッタ・石 などの 役者は、その駅へ 巻きもどした ときと 同じ 形に もどす（起こした ねこも また ねて いる。これは ふだん 失敗して 巻きもどした ときと 同じ）。うしろの ものは もう かかわらない
  - ホームで まつ 乗客: もう 乗った 人は ホームに もどさない（つづきでも、失敗して 巻きもどした ときも。2-2 ミッション 2 で クローバーえきに 2 人 のこって 見えて いた）
- **早送り（`fastForwardCutscene`）**: 残る ものだけ 一瞬で 済ませる: 線路を 切る（`rail:cut` に `instant`。飛んで いく 線路の かけらや 落ちる 橋は 出さず、`fall` の 飾りは そのまま 消す。音も 鳴らさない）、能力（ボタンが 出るだけ。「〜を おぼえた！」の 札は 出さない）、役者の 出し入れ（出てきた ものは さいごに 動いた 場所に 1 回だけ 出す。すぐ 消える ものは 出さない。▶▶ の 直前に 出て まだ モデルを 読んで いる ものは、画面の がわで 「動く」を 読みおわりまで 待たせ、「消す」が 先に きたら 出さない）。せりふ・待ち・札・字幕・カメラ・効果（くしゃみ）は とばす。おわったら 止まった カメラも ふつうに もどる
- **タイトル**: つづきが あれば 大きい ボタンは「つづきから（2-1 ミッション 2）」（18 字）。小さい ボタンの「はじめる」は「はじめから（2-1）」に なり、その ステージを はじめから 遊べる。ちがう ステージの タイトル（ふつうは 1-1）からは そのステージを ひらき なおす（`?stage=2-1&go=1&resume=1`）。同じ ステージなら その場で はじまる。「つづきから（つぎの ステージ）」より つづきが 先
- **▶▶（7）**: クリアしたことの ある ステージの 寸劇（オープニング・ミッションの あと・おわり）の 間だけ、右上の 角の 列の いちばん 内側（カメラの となり、同じ 大きさ、黄色い ふち）に 出る。いつも 見える ボタンでは ないので「丸ボタン 4 つまで」には 数えない。押すと のこりを 早送りして、つぎの 札へ とぶ
  - 2 度 押し: 最初に 触れた ときに すぐ かくれて、それ から 先は 何も しない。とんだ あと 1.5 秒 以内に 出る 札は、ボタンが 0.8 秒 おくれて 出る（ミッションの 札を 見ないまま 閉じない）。▶▶ の 場所には ほかの ボタンが ないので、2 回めの タップは 何にも あたらない
  - 札（寸劇の 札、能力の 札）が 出て いる 間は かくれる（札は 子どもの タップで 閉じる もの）。一時停止中も かくれる
- テスト: `tests/smoke/resume.spec.ts`（セーブを 入れて）: 1-1 の タイトルから 1-2 ミッション 2（オープニングの 早送りで ジャンプを 覚える。ライトは まだ）、2-2 ミッション 3 を その場で（花 1・2 が ひらいて いる、草が 大きい、乗客 0）と ミッション 2（乗客 2）、2-3 ミッション 3（ロケットを 早送りで 覚える → 下り坂・かんそくじょ・カウントダウン → みさき → おわりの 寸劇を ▶▶ → 橋が 切れて いる・サカサが いない・止まった カメラでない → クリア札（つづきの 前の 停車と 記録も 数える） → つづきが 消える）、1-1（クリア前は ▶▶ が 出ない。さくらえきで つづきが 書かれる（停車の かず つき）。つづき ミッション 3 の まま はじめから 遊ぶと、さくらえきの あとも ミッション 3 の まま。クリア後は ▶▶ で 最初の ミッションの 札へ。2 度 押しでも 札は 残る。その 遊びなおしでは 2-1 の つづきが 残る）

## 5. 進め方（PR の分け方）
1. **2-3 の PR に入れる**: カメラを右上へ、ロケットの席（§1）
2. **仕上げ PR1 軽量化**: §2 の A〜C と `npm run budget`。2-2・2-3 が予算を超えそうなら、2-2・2-3 より先にやる
3. **仕上げ PR2 章のおわり**: §3（1 章・2 章のおわり、「？」の島、タイトルの★）
4. **仕上げ PR3 きろく**: §4 の 1・2・4
5. **仕上げ PR4 入口のみがき**: §4 の 5・6・8・9・10
6. **仕上げ PR5 つづき**: §4 の 3・7（一番あぶないので最後）

どの PR も、本番 `https://hassy0511.github.io/train-game/`、テストコース `https://hassy0511.github.io/train-game/?stage=0-0`、モデル確認 `https://hassy0511.github.io/train-game/models.html?model=<名前>` で確かめられるようにする。

## 6. だいさんに決めてほしいこと（おすすめ付き）
1. 1 章のおわりにも小さい札を出すか → おすすめ: 出す
2. 3 章の予告 → おすすめ: 2-3 のあとに「？」の島を 1 つだけ出し、タップで「つづきは また こんど！」
3. 「とちゅうから つづける」をこの仕上げに入れるか → おすすめ: 入れる。ただし最後の PR にする
4. 1-1 の記録 3 つ → おすすめ: まちの かんばん／ほんぶの せっけいず／やねの うえの ふうせん（ジャンプ）
5. ロケットの記録の取り方 → おすすめ: 急坂の短い支線（2-3 の急坂を使い回す）

ということで、上の 5 つを決めてほしい（全部おすすめのままでよければ「おすすめで」の一言で進められます）。